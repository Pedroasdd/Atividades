#include<stdio.h>

double maior(double a, double b){
    if(a > b){
        return a;
    }
    else{
        return b;
    }
}

int main(){

    double a, b;
    scanf("%lf %lf", &a, &b);

    double maiornum = maior(a,b);

    printf("%.lf\n", maiornum);

    return 0;
}
